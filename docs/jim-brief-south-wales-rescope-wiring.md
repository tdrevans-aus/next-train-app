# Jim brief — wire the re-scoped South Wales catalog (16 stations) and fix the Wales / West of England bounds overlap

**Date:** 7 Sep 2026 · **Lane:** expansion adapter lane (Jim), then Mark · **tim-review:** no (Tim asked for this on 6 Sep; merge when Mark is green). South Wales is already `status: "live"` — this is a catalog extension of a live region, an ordinary PR, **not** a flip PR.
**Dispatch:** `subagent_type: jim`, `isolation: "worktree"`. Start from `origin/south-wales-rescope-pack` (Luke's pack, includes Nico's rewritten report): `git fetch origin && git checkout -b south-wales-rescope-wiring origin/south-wales-rescope-pack`. Run `node qa/lane-lock.mjs acquire gb south-wales jim south-wales-rescope-wiring` before touching shared files (the top-level check reported `gb: free` at dispatch).

## Input
`docs/south-wales-d1/jim-handoff.md` (Luke, 7 Sep 2026) is the spec for `lib/cities/south-wales/stations.json` and the region config. Where the handoff says the registry status should be `planned`, ignore it: the region is live and stays live. `docs/south-wales-d1/oracle-clash-report.md` "Re-scope 7 Sep 2026" section has the 16 live-probed CRS codes (BYI for Barry Island, CPH for Caerphilly — not BRI/CAE).

## Also fix — GPS bounds (found while checking Luke's coordinates)
`public/city-session.js` `CITY_BOUNDS`:
- `south-wales` is `{ minLat: 51.30, maxLat: 51.70, minLng: -3.65, maxLng: -2.70 }`. Swansea (-3.94), Neath (-3.81), Port Talbot Parkway (-3.78), Merthyr Tydfil (51.74), Rhymney (51.76) fall outside it. Widen to cover all 16 stations with a small margin (about `{ minLat: 51.35, maxLat: 51.80, minLng: -4.05, maxLng: -2.70 }`), then check every South Wales coordinate is inside.
- `west-of-england` is `{ minLat: 50.90, maxLat: 51.95, minLng: -3.20, maxLng: -2.10 }`, which contains Cardiff (51.476, -3.179), Newport and Barry. `hintCityFromCoords` returns the **first** matching box in object order and `west-of-england` precedes `south-wales`, so a rider in Cardiff is hinted to West of England today (PR #318 made the app follow that hint on first load, so this now matters). Pull West of England's western edge back to the Severn — check its six catalogued stations' coordinates in `lib/cities/west-of-england/stations.json` and set `minLng` just west of the westernmost one (Bristol is -2.58; if Weston-super-Mare (-2.97) or Taunton (-3.10) are catalogued, keep them but exclude the Welsh shore by lat/lng, e.g. two boxes are not supported, so choose the tightest single box that holds West of England's stations and excludes Cardiff/Newport, and document the trade-off in a comment).
- Add `qa/uk-city-bounds-overlap-gate.mjs` (smoke tier): for every UK region, every catalogued station with coordinates resolves via `hintCityFromCoords` to **its own** region (parse `CITY_BOUNDS` from city-session.js the way `qa/live-city-lists-sync.mjs` parses `COUNTRIES`); report any station that resolves elsewhere, and fail. Expect pre-existing overlaps to show up (Walsden WDN between West Yorkshire and Greater Manchester, boundary stations like Severn Tunnel Junction or Chesterfield) — fix the boxes where a tighter box does it, otherwise add an explicit allow-list entry in the gate with a one-line reason per station, never a silent skip.

## Acceptance
1. `lib/cities/south-wales/stations.json` has the 16 stations with CRS, lat/lng, notes updated; `notes` no longer claims Valley Lines need a separate feed.
2. `node qa/south-wales-dogfood-gate.mjs` updated for 16 stations (hub CDF, secondary CDQ, no doNotGroup, Swansea boundary, Llanelli/Carmarthen must NOT resolve) and green; `node scripts/probe-uk-board.mjs "Pontypridd" --region=south-wales` returns a board.
3. `hintCityFromCoords(51.476, -3.179)` → `south-wales`; `(51.4545, -2.5879)` Bristol → `west-of-england`; `(51.6251, -3.9415)` Swansea → `south-wales`. Add these to the new bounds gate or `qa/region-selection.mjs`.
4. `node qa/uk-city-bounds-overlap-gate.mjs` green; `node qa/run-all.mjs --smoke` green.
5. No `MULTI_CITY_IDS` / registry status / live-list changes (region is already live). Commit (Co-Authored-By trailer as usual), push `south-wales-rescope-wiring`, open a PR linking this brief and the pack, then release nothing by hand — the lock auto-releases on merge.
