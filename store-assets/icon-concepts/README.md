# Icon concepts (pick one)

Teal brand `#0B6E6A` / mist `#EEF3F2`. Current toy-train mark is retired once you pick.

| ID | Idea | Files |
|----|------|-------|
| **A** | Bold **N** monogram | `A-n-mono.svg` + AI draft `exports/icon-concepts/A-n-mono.png` |
| **B** | Mist field + teal badge: **clock + train** (leave-by) | `B-leave-clock.svg` + AI draft |
| **C** | Rising **signal bars** (next departure) | `C-signal-bars.svg` + AI draft |
| **D** | Cleaner **side train** mark | `D-train-mark.svg` + AI draft |
| **E** | **12 MIN** countdown (most “leave-by”) | `E-leave-12.svg` |

Ship path: say the letter → Simon replaces `public/icon.svg`, re-exports `store-assets/exports/play-icon-512.png`, syncs Android mipmaps if needed.

**Simon lean:** **E** or **C** for store shelf distinctiveness; **A** if you want brand letter; **B** if leave-by story must be literal.
