# UK station catalog (seed)

CRS codes for Darwin OpenLDBWS. **Not used by the live app** until `docs/uk-provider-design.md` launch gate is cleared.

Expand with family/friends commute pairs before enabling. Full CRS list: National Rail station list or Darwin `GetStationList` on the reference service.
