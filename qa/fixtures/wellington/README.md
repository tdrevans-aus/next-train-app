# Wellington fixtures

GTFS TRAIN snapshot in `gtfs/` (`node scripts/trim-wellington-gtfs.mjs`).

`published-network.json` is a **verbatim copy** of `docs/wellington-d1/published-network.json`. Hand-transcribed from the Metlink Regional Rail Network map. **Never generate it from GTFS.**
