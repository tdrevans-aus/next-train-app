const fs=require('fs'); const p='.env.local'; const st=fs.statSync(p); console.log('mtime', st.mtime.toISOString(), 'bytes', st.size); const t=fs.readFileSync(p,'utf8'); t.split(/\r?\n/).forEach((l,i)=>{ const s=l.trim(); if(!s) return; if(s.startsWith('#')) { console.log(i+1, 'comment'); return; } const eq=s.indexOf('='); if(eq<0){ console.log(i+1, 'no_eq'); return; } const n=s.slice(0,eq).trim(); let v=s.slice(eq+1).trim(); const quoted=(v.startsWith('"')||v.startsWith("'")); if(quoted) v=v.slice(1,-1); console.log(i+1, n, 'vlen='+v.length, quoted?'quoted':'bare', v==='[SENSITIVE]'?'stub':''); });
