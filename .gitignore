node_modules/
.vercel
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/app/release/
*.iml
.idea/
*.jks
*.keystore
keystore.properties
android/keystore.properties
.env*

public/site-config.json
public/dogfood-origin.json

public/_*.txt

qa/reports/*.json
qa/tmp/
qa/*.png
qa/*.xml
!qa/maestro/

docs/*.xlsx

# Root-level scratch output from ad-hoc QA/debug runs — never source
/qa-*.log
/qa-*.exit
/_out*.txt
/_ls_raw.bin
/git-out.txt
/test-*-out.txt
/test-output-*.txt
/tmp-*.js
/tmp-*.mjs

# api/package.json is a bundling stub; local npm installs regenerate a
# lockfile there we don't want to track (no other package.json in the repo
# carries one either — the root package-lock.json is the real one).
/api/package-lock.json
