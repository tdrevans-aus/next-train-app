# Copy to android/keystore.properties (gitignored) for CLI signed release builds.
# Never commit keystore.properties or passwords.
storeFile=C:/Users/tdrev/keys/nexttrain-upload.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=nexttrain
keyPassword=YOUR_KEY_PASSWORD
